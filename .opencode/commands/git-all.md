---
description: Stage all changes and create a semantic commit message automatically
agent: build
---

Staged Git Diff:
!`git diff --cached`

Analyze the diff above and generate a clean, precise conventional commit message following the Conventional Commits specification.
Write ONLY the commit message and nothing else. No markdown formatting.
