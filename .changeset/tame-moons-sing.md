---
"boltdocs": patch
---

fix: move `react-router-dom` to `ssr.noExternal` to resolve "module is not defined" in Vite 8 SSR module runner; add `overflow-x-auto` to tabs for mobile horizontal scroll; increase docs-layout mobile padding to `px-4 sm:px-6`
