---
'create-boltdocs': patch
---

Update scaffolded `package.json` — add `react-aria-components` as dependency (now a required peer of `boltdocs`) and remove `sharp` from `onlyBuiltDependencies` (core no longer depends on it).
