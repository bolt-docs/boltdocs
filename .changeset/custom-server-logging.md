---
'boltdocs': patch
---

Replace Vite's default URL output with a branded box in `dev` and `preview` commands. Suppresses Vite's info-level logging to show a clean `boltdocs`-styled box with Local and Network URLs instead.
